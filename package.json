{
  "name": "finkas",
  "version": "1.0.0",
  "private": true,
  "description": "Finkas - Modern PWA Kas & Iuran",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./css/input.css -o ./style.css --minify",
    "dev:css": "npx @tailwindcss/cli -i ./css/input.css -o ./style.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
